const encryptService = require('../services/encryptionService')

// Расшифрование приходящих данных
module.exports = function (req, res, next) {
    if (req.body.value) {
        const encryptedData = req.body.value;
        req.body = encryptService.decrypt(encryptedData)
    }
    next()
}
const knex = require("knex")

module.exports = knex({
    client: 'pg',
    connection: {
        host: 'localhost',
        port: '5432',
        user: 'postgres',
        password: 'Popgen999#',
        database: 'test'
    }
})
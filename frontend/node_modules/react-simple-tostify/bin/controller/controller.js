const express = require('express')
const authController = require("./controls/authControls")
const commonController = require("./controls/commonRoutesControls")
const mainRouter = express.Router();

mainRouter
    .use(authController)
    .use(commonController)

module.exports = mainRouter